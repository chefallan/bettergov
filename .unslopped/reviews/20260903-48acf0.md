[major] Only public/locales/en/common.json was rebranded; the other 12 locales (fil, ceb, war, ilo, hil, pam, pag, bcl, tsg, mag, mdh, krj) still greet visitors with "Welcome to BetterGov.ph" and a 2025 BetterGov.ph copyright, so any non-English language switch shows the old brand (the intentional fil assertion at e2e/critical-flows.spec.ts:66 depends on this and must be updated together with the fil locale).
[major] src/data/seo-metadata.json only had its "/" entry updated; about 173 remaining "BetterGov.ph | Republic of the Philippines" titles and descriptions mean every other route still serves old-brand national SEO metadata.
[major] public/sitemap.xml was regenerated with lastmod 2026-09-04 but all 92 URLs still use https://bettergov.ph while src/components/SEO.tsx now emits https://betterkananga.ph canonicals, sending search engines contradictory domain signals; regenerate the sitemap from SITE.baseUrl.
[minor] package.json moved the ws override from the exact pin 8.20.1 to the floating range ^8.21.0, so any install without the lockfile (or a future lockfile refresh) silently pulls whatever newer 8.x ws is published, which widens supply-chain exposure if a bad release lands in that range; pin the exact patched version instead (the lock currently resolves 8.21.3).
[minor] src/components/layout/Navbar.tsx and src/components/layout/Footer.tsx still render the old logo files /logos/svg/BetterGov_Icon-Primary.svg and /logos/svg/BetterGov_Icon-White.svg while the alt text now says "BetterKananga logo", so the visible brand mark contradicts the new name; swap in a Kananga logo asset or rename/replace the SVGs.
[minor] src/components/SEO.tsx keeps /logos/jpg/BetterGov_Vertical-White.jpg as the default ogImage, so social shares of betterkananga.ph pages show the old BetterGov logo; move the image path into SITE config and point it at a new asset.
[minor] src/components/layout/Navbar.tsx lines 161 and 389 still link "About" to https://about.bettergov.ph, sending users to the old national brand's site; point these at a local about page or the new project's page.
[minor] src/components/layout/Footer.tsx line 67 hardcodes "Better Kananga" with a space instead of using the new SITE.name config, leaving a second source of truth for the brand string; derive it from src/config/site.ts.
[minor] public/llms.txt only had its "Last Updated" line bumped to 2026-09-04 while the entire file still describes the national BetterGov.ph portal with bettergov.ph URLs, which falsely presents stale national content as fresh; rewrite it for BetterKananga or leave the date untouched.

Resolved since the first review pass (commit 6cd8583 "fix(brand): align e2e specs and date timezone with the rebrand"):
- The Playwright specs in e2e/homepage.spec.ts, e2e/performance.spec.ts, and e2e/critical-flows.spec.ts now assert BetterKananga; verified the only remaining BetterGov assertion is the intentional Filipino hero check at e2e/critical-flows.spec.ts:66, which stays until the fil locale is rebranded.
- src/lib/utils.ts formatDate now formats in Asia/Manila instead of UTC, so Philippine visitors see the local date; verified the utils unit test still passes machine-independently since UTC-parsed dates land at 8 AM Manila on the same calendar day.

verdict: no critical findings
