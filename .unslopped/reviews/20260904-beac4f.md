[major] Every page now ships duplicate head tags because App.tsx line 103 mounts a global <SEO /> while the 13 converted pages and the pages passing <SEO> props render their own title and metas; Helmet deduped these with the page winning, but React 19 hoists all of them, and since metas append to head in mount order the generic App-level description and og tags sit first, so scrapers that read the first occurrence get the generic values instead of the page-specific ones. Titles still display correctly because React inserts a new title before the existing one, which hides the problem. Fix by removing the global <SEO /> or the page-level duplicates.
[minor] index.html carries static og:image (a relative path, invalid per the og spec), og:image dimensions and og:type that duplicate the React-rendered og tags and sit earliest in head; this predates the commit since Helmet never removed them, but they should go now that React owns these tags.
[minor] src/pages/flood-control-projects/contractors/[contractor-name].tsx line 644 gives <title> two JSX children ({contractor.value} plus text); React 19 expects a single string child, this happens to work in client rendering as concatenated text nodes but warns and breaks under SSR; use a template literal.
[minor] The inline application/ld+json scripts in SEO.tsx are not hoisted by React 19 and now render inside the body instead of head; script elements are invisible so nothing breaks visually, and Google accepts JSON-LD in body, so this is acceptable as is; add a code comment so nobody moves it back expecting hoisting.
[minor] npx tsc -p functions/tsconfig.json fails with 28 errors, but the identical errors occur under workers-types v4.20250607.0, so the ^5 bump introduces none of them; functions:build was already broken and deserves its own cycle.
[minor] .unslopped/DEBT.md line 17 still lists the react-helmet-async production rendering debt this commit resolves; sweep or remove the entry.
verdict: no critical findings
