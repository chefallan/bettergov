[minor] src/pages/kananga/index.tsx lists the eight councilors by ballot nickname ("An-An Napari", "Imo Palar", "Richie Cruz") while the mayor and vice mayor get full legal names, so the official-looking roster mixes name styles; show full names or label the list as ballot names like the officials.json ballotName field does for the mayor.
[minor] The "richest municipality in Eastern Visayas" claim in src/pages/kananga/index.tsx is an undated superlative stated as permanent fact; anchor it to the COA report year the cited PNA article covers (e.g. "per the COA 2023 annual financial report") so it does not silently go stale.
[minor] The barangay table in src/pages/kananga/index.tsx has th cells without scope="col", so screen readers may not associate headers with cells; add scope attributes.
[minor] The barangay table has no caption or aria-label tying it to the "Barangays" heading; add a visually hidden caption such as "Barangays of Kananga with 2020 census population" so the table is self-describing to assistive tech.
[minor] The new /kananga route is not in public/sitemap.xml (the sitemap also still lists bettergov.ph URLs, flagged in the previous review); include /kananga when the sitemap is regenerated.
[minor] The navbar.kananga key was added only to public/locales/en/common.json; the other 12 locales fall back to English, which happens to render the proper noun "Kananga" correctly, but add the key to the other locale files for consistency when they are rebranded.
[minor] The entire /kananga page body in src/pages/kananga/index.tsx is hardcoded English with no i18n keys on a site shipping 13 locales; move the labels and prose into locale files or note the page as English-only by design.

Verified clean: the 23 barangay populations in src/data/kananga/barangays.json sum to exactly 59696; officials.json matches the checked facts (mayor Manuel Vicente M. Torres, vice mayor Miguel Jorge P. Tan, 8 PFP councilors in descending vote order, term 2025 to 2028); all imports resolve (App.tsx route, JSON modules, test alias) and npx tsc --noEmit passes; both new test files pass (8 tests); sources are labeled on the page and external links use rel="noopener noreferrer"; the new navbar item breaks no e2e assertion since e2e/utils/navbar.ts and the specs locate nav links by name, never by index or count, and both desktop and mobile navbar branches handle a children-less item.

verdict: no critical findings
