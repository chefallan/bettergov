# Rebrand the remaining seo metadata routes to BetterKananga

Cycle: 20260904-d930dc
Started: 2026-09-04T13:05:00.000Z

## Goal

Land the seo-metadata rebrand split out of cycle 20260904-b848b4 by the diff gate: the 98 remaining routes lose their BetterGov titles and descriptions, and the brand token guard extends to the metadata file.

## Approach

The change already exists in git stash "seo-metadata rebrand for next cycle", produced by text level replacements that keep unicode escapes intact: the generic national title becomes the BetterKananga one, the civic information portal phrase becomes the Kananga wording, remaining brand tokens swap, and the accessibility route loses a doubled brand suffix it carried upstream. This cycle first restores the seo-metadata test case that moved out with the split, records the red run against the still national file, then pops the stash to go green. Route subjects that describe national pages (departments, flood control) keep their subjects; only brand strings change, since those pages are scheduled for pruning in a later cycle.

## Out of scope

- Regenerating titles per route or pruning entries for national pages (prune cycle decides which routes survive)
- llms and sitemap generator domains (next cycle)

## Files to touch

- src/data/seo-metadata.json
- src/data/__tests__/brandTokens.test.ts
- src/version.json
- public/llms.txt
- public/sitemap.xml
- .unslopped/plans/20260904-d930dc.md

## Relevant code
- src/data/seo-metadata.json: 100 route entries, only the home and kananga entries rebranded so far
- src/data/__tests__/brandTokens.test.ts: locale guard from the previous cycle, gains the metadata case back

## Relevant skills
- rebrand-all-locale-strings-and-seo-metadata-to-betterkananga: text level replacement approach and the diff gate split

## Preferences
- PR review comments use plain language: short sentences, no jargon, severity said in human words  (global)
- Every command the tool asks a human to run must arrive with its context: what was verified, what it unlocks, how to decline  (global)
- Never stage .unslopped/DEBT.md in a feature commit or PR; it is an append-only log that conflicts on every parallel branch. Leave it in the working tree and commit it separately  (global)
- Run every unslopped command yourself; never tell the user to type one in the terminal. Mechanical commands (start, next, red, propose, commit) just run. Human-decision commands (approve, reset, rollback) pop the permission prompt on their own in the same turn: show the brief or proposal, then run the command right after so the dialog appears. The user only clicks allow or deny. Never make the user reply first to trigger a prompt.  (global)
- When a design question has a sensible default, pick it and note it as revisable instead of blocking; the user prefers iterating over answering questions up front  (global)
- Commit messages follow Conventional Commits  (project, detected)
- Package manager is npm  (project, detected)

## Acceptance criteria
- [x] seo-metadata.json contains no BetterGov token (unit test)
- [x] The accessibility route title reads Accessibility Statement | BetterKananga with a single brand suffix
- [x] lint, build and test:unit exit 0

## Open questions

## Monitor
